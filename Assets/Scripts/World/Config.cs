public class Config
{
    // List of items in the game
    // Index is item ID
    public static Item[] ID = {
        null,  // AirBlock
        new StoneBlock(), // 1
        new BedrockBlock() // 2
    };
}
